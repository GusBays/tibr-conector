# API integração Agis -> Bagy

Para iniciar, crie o .env na raiz do projeto baseado no exemplo, depois rode:

```
docker compose up -d --build
```

Para visualizar logs de algum serviço:

```
docker compose logs -f {{api|mysql}}
```

Para parar o serviço:

```
docker compose down
```
